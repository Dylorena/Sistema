export enum AttrType { Aggregate, Attribute, Collection, Dynamic, List, Reference };

export enum DataType {
  String, Char, Int, BigInt, SmallInt, Decimal, DateTime, Date, Time, Binary, Memo, Image
};